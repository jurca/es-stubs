/**
 * 
 *
 * @summary 
 * @template E
 * @param {E} searchElement The element to search for.
 * @param {number=} fromIndex
 * @return {boolean} <p>A <code>Boolean</code>.</p>
 * @see TypedArray.prototype.includes()
 * @see String.prototype.includes()
 * @see Array.prototype.indexOf()
 */
Array.prototype.includes = function (searchElement, fromIndex) {};

/**
 * 
 *
 * @summary 
 * @template E
 * @param {E} searchElement The element to search for.
 * @param {number=} fromIndex Optional. The position in this array at which to
 * begin searching for <code>searchElement</code>; defaults to 0.
 * @return {boolean} <p>A <code>Boolean</code>.</p>
 * @see Array.prototype.includes()
 * @see String.prototype.includes()
 * @see TypedArray.prototype.indexOf()
 */
TypedArray.prototype.includes = function (searchElement, fromIndex) {};
